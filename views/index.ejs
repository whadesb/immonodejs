<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil - UAP Immo</title>
    <style>
      /* Reset des styles par défaut */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      /* Styles généraux */
      body {
        font-family: Arial, sans-serif;
        background-color: #f8f8f8;
        color: #333;
        line-height: 1.6;
        margin: 0;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
      }

      /* Barre supérieure transparente */
      .top-bar {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        z-index: 1000;
        padding: 10px 0;
      }

      .top-bar .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 0 auto;
        padding: 0 20px;
      }

      .top-bar .logo {
        font-size: 1.5rem;
        color: #333;
      }

      .top-bar .auth-links {
        color: #333;
      }

      /* Menu */
      .menu {
        position: absolute;
        top: calc(40px + 20px); /* Ajout de 20px de marge pour correspondre à la top-bar */
        left: 0;
        right: 0;
        background-color: #fff;
        border-radius: 20px; /* Arrondir tous les bords */
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        z-index: 1000;
        padding: 20px; /* Ajout de padding */
        margin: 0 40px; /* Ajout de marges gauche et droite */
      }

      .menu .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 0 auto;
      }

      .menu ul {
        list-style: none;
        display: flex;
      }

      .menu li {
        margin-right: 20px;
      }

      .menu li:first-child {
        margin-left: 0; /* Supprimer la marge gauche du premier élément */
      }

      .menu li a {
        text-decoration: none;
        color: #333;
        transition: color 0.3s ease;
      }

      .menu li a:hover {
        color: #ff7f50;
      }

      /* Section Colonne */
      .column {
        background-color: #ff7f50;
        color: #fff;
        padding: 40px;
        border-radius: 10px;
        margin-top: 20px;
        text-align: left;
      }

      .column:hover {
        background-color: #ff6347; /* Changement de couleur au survol */
      }

      .column h3 {
        font-size: 2rem;
        margin-bottom: 20px;
        color: #fff;
      }

      .column p {
        font-size: 1.2rem;
        margin-bottom: 20px;
        line-height: 1.6;
      }

      .column ul {
        columns: 2;
        -webkit-columns: 2;
        -moz-columns: 2;
        list-style: none;
        margin-bottom: 20px;
      }

      .column ul li {
        margin-bottom: 10px;
      }

      .column .btn {
        display: inline-block;
        padding: 10px 20px;
        background-color: #fff;
        color: #ff7f50;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s ease, color 0.3s ease;
      }

      .column .btn:hover {
        background-color: #ff6347;
        color: #fff;
      }

    </style>
</head>
<body>

  <!-- Barre supérieure transparente -->
  <div class="top-bar">
    <div class="container">
      <div class="logo">UAP Immo</div>
      <div class="auth-links">
        <a href="/connexion">Connexion</a> |
        <a href="/inscription">Inscription</a>
      </div>
    </div>
  </div>

  <!-- Menu -->
  <div class="menu">
    <div class="container">
      <ul>
        <li><a href="#presentation">Présentation</a></li>
        <li><a href="#temoignages">Témoignages</a></li>
        <li><a href="#annonces">Annonces</a></li>
      </ul>
    </div>
  </div>

  <!-- Section Colonne -->
  <section class="column">
    <h3
